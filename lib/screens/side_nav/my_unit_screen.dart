import 'package:flutter/material.dart';

class MyUnitScreen extends StatelessWidget {
  const MyUnitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Unit Screen'),
      ),
    );
  }
}
